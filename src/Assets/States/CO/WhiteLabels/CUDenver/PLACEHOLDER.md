# CUDenver_Logo_White.png is a PLACEHOLDER

This file currently holds a color reference image, NOT the production logo.

Replace it with the **white** CU Denver campus mark as a **transparent RGB PNG (~2x)**
(exported from the vector master; the `.eps` CU Denver provided is CMYK/print and
cannot be imported directly). The white variant is required because the themed
header renders **black** (see the `cu_denver` theme in `styleController.ts`).

Ticket: MFB-1317
